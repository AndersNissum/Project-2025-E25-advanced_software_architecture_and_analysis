Flask==2.1.2            # Web framework for building the application
Werkzeug==2.0.3
Flask-Cors==3.0.10      # For handling Cross-Origin Resource Sharing
requests==2.26.0        # For making HTTP requests (if needed)
prometheus_flask_exporter==0.18.0  # For exposing metrics to Prometheus
pandas==1.3.4           # For data manipulation and analysis (if needed)
numpy==1.21.2           # For numerical operations (if needed)